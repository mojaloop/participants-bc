"use strict"

describe('example test', () => {

  beforeEach(async () => {
    // Setup
  })

  afterEach(async () => {
    // Cleanup
  })

  test('should goes here', async () => {
    await expect(true)
  })
})
